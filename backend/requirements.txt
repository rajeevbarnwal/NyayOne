# Backend runtime dependencies — WHAT the backend needs.
#
# The exact versions live in requirements.lock — WHICH versions were tested —
# and the constraint line below applies them, so
#
#     python -m pip install -r backend/requirements.txt
#
# resolves to the same set that produced the green complete backend suite and
# the green Wave 2 browser gate, transitive packages included.
#
# NO core runtime package floats any more. Every distribution named here is
# pinned in the lock, and both runners refuse to start against an interpreter
# that does not match it (backend/scripts/check_runtime_lock.py).
#
# Adding a dependency means editing BOTH files in the same commit: the name
# here, the exact version there.
-c requirements.lock

fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
httpx==0.28.1
sqlalchemy==2.0.51
# SQLAlchemy's transitive greenlet marker excludes some supported platforms
# (including macOS arm64), while our runtime lock requires one exact,
# interpreter-independent package set on every host.  A constraints file does
# not install missing distributions, so keep this dependency direct.
greenlet==3.5.4
alembic==1.18.5
psycopg[binary]==3.3.4
pgvector==0.5.0
cryptography==45.0.7
